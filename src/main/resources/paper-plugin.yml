name: BlockEngine
prefix: BlockEngine
version: '${version}'

main: dev.auto.blockengine.Main
bootstrapper: dev.auto.blockengine.MainBootstrap
loader: dev.auto.blockengine.MainLoader
api-version: '26.1'
load: STARTUP

authors: [ AutoYT ]

permissions:
  blockengine.debug:
    description: Allows access to BlockEngine debug commands.
    default: op
